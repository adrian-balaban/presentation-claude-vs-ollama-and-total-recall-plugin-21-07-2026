* ../total-recall.md 
** 
** corectare/adaugare in README 
  inspirat din alte pluginuri cu acelasi nume si dat lista
  sters/corectat paragrafe cu aceste pluginuri
  specificat ca sunt si altele  
** actualizare *.md
** proba mii de fisiere: download 1- 2 sites -> incarcare ca memorii locale si org
** instalare si proba

* prezentare.ro.md
** review
** adaugare referinte incrucisate intre cele 2 repo-uri
** adaugare .md in engleza


* pregatire pt prezentare
** DONE Verified — here's how to see it rendered exactly like the real presentation:
npx @marp-team/marp-cli prezentare.ro.md --html -o prezentare.html --allow-local-files
** TODO Then open prezentare.html in a browser. Use ←/→ or click the arrows at the bottom to navigate slides (it opened directly to "Slide 5" with the Razer image, confirmed working).

Important: run this from inside the prezentare-claude-vs-ollama... folder (or -o to a path inside it) — the image paths (images/*.png) are relative to wherever the HTML file ends up, so if you output it elsewhere the pictures break.

Faster alternative for day-to-day editing: install the Marp for VS Code extension — it gives you a live side-by-side preview in the editor that updates as you type, no CLI command needed each time.

(I generated a one-off prezentare.html in the folder to verify this works, then deleted it — nothing was left behind in your repo.)